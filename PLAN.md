# Plan: 第三方登录配置页面（分阶段交付）
_Locked via grill — Codex + 用户_

## Goal
在 `runtime-ui/modules/authentication-manager-ui` 独立新增“第三方登录”模块和同级菜单，保留现有“单点登录”。第一阶段在本地/评审环境交付可查看、可操作、符合平台视觉与 Vue 3 组件体系的配置页面，初次进入为空列表、由管理员从“新增”开始体验；页面使用内存状态而非假接口或持久化，明确告知当前操作不会生效。待用户看过页面并确认布局后，第二阶段才做真实配置接口与前后端联调；随后开展实际第三方登录、登录页接入和日志审计。

## Approach
1. 在 `views/system/ThirdPartyLogin/` 放独立页面与必要的表单/状态逻辑；在 `baseMenu.json` 于 `system/Apply` 同级新增唯一路由与“第三方登录”菜单模板，不变更原菜单、旧页面、登录认证链路及其他模块。核对现有动态路由发现、菜单上下文、i18n 与组件导出。该 JSON 是菜单初始化模板，真实菜单可见性取决于管理端同步；仅在本地/评审环境由有权限的管理员同步与预览，不将未联调页面同步到生产菜单或部署给普通用户，也不捏造后端按钮权限。
2. 实现平台容器内的双栏布局：左侧微信、钉钉、企业微信、OAuth2 固定方式筛选；右侧配置列表、名称/标识搜索、“新增”主操作、状态和空态。从零条记录开始；允许同一种方式新增多条配置。使用现有 `EqualHeightColumns`、Ant Design Vue 列表/表单、`JlDrawerShell`、`CloudEmpty`、状态组件及平台样式 token，不复制参考 HTML 的顶栏、全局导航、行内 CSS 或虚拟组件。
3. 抽屉中提供共通名称、登录页显示名称、图标，以及按方式变化的字段：微信应用类型/AppID/AppSecret；钉钉 CorpId/AppKey/AppSecret/组织架构与成员同步开关；企业微信 CorpId/AgentId/Secret；OAuth2 授权、令牌、用户信息地址、client_id/client_secret、scope、用户唯一标识。提供必要的必填/格式校验；Secret 用密码框，明确禁止输入真实凭据，只允许示例值；关闭/卸载清理表单状态。图标上传在本阶段仅做本地选择与预览，不上传。
4. 内存状态支持新增、编辑、删除确认、按方式筛选、文本搜索、启停、登录页显示开关、钉钉同步开关；列表与抽屉状态一致。刷新重置为空；不使用 localStorage，不保存真实密钥，不发网络请求、不记录凭据日志。页面持续显示克制的“当前仅供预览，配置不会保存或影响登录/同步”提示，每项操作反馈仅指向本页状态；离开/刷新前若存在未持久化的编辑，给出数据丢失提醒。
5. 本阶段不做登录页效果预览、测试登录、登录策略、登录日志、实际登录、真实回调地址及数据同步。将后续任务按依赖顺序写在新目录 `TODO.md`：先审查现有 `system/Apply` 与新身份源配置的接口/数据关系，决定复用、迁移或并存，再完成配置接口/安全存储/上传与回调地址/组织同步联调 → 登录流程及登录页接入 → 日志审计。同步在模块既有说明文档记录页面边界。
6. 验证空态→新增四类→筛选/搜索→编辑/启停/展示/删除→刷新重置及离开提醒、敏感字段不落本地存储；校验菜单 JSON 和旧菜单不变、页面类型与构建；在可访问的本地运行环境中向用户展示实际页面效果，无法进行登录态浏览器验收时说明限制。用户确认第一阶段视觉与交互后，才进入第二阶段真实接口设计与开发。

## Key decisions & tradeoffs
- 采用“方式筛选侧栏 + 配置列表”的平台化管理工作区，而不是参考页的全局壳或现有单点登录卡片墙。业务第一任务是按登录方式管理多条配置；右侧紧凑列表为唯一主视觉锚点，状态优先、动作就近、无 KPI/banner。左侧用浅层筛选选中态而不是复制导航树。
- 搜索仅为名称/标识固定文本项，选用平台现成输入控件；不引入不需要的组合查询 `ProSearch`、统计卡或保存搜索。完整凭据采用抽屉分区表单；单项启停/展示直接在列表操作并作确认，避免每次打开全字段表单。类型字段不在编辑时切换。
- 第一阶段是生产代码结构的 UI 实施，不等于已接通的生产认证能力：可见菜单服务于评审，部署/对外宣称可用须等第二、三阶段。模拟 Secret 不能伪装成后端加密存储；不展示可复制的假回调地址。
- 四类方式与字段取自参考 HTML 的业务信息；它的种子数据切换、假测试结果、登录页预览、策略/日志空按钮均不照搬。初始数据为空，用户从新增开始。
- 方案档案：`标准管理表格页 · 方式筛选侧栏变体`。第一任务是定位某种登录方式的配置并维护状态；属于字段固定、需要对比关键标识/状态/更新时间的轻量配置管理，故不用卡片台账（单卡无法高效横向对比）、不用高频组合条件筛选工作台（只有方式与单一文本条件）。首屏结构：

  ```text
  ┌─────── 平台已有导航与页面容器 ───────────────────────────────────────────┐
  │ [第三方登录]                                      [名称/标识] [新增]   │
  │ ┌─ 登录方式 ────┐  ┌─ 配置列表 / 空态（唯一主内容）─────────────────┐ │
  │ │ 全部          │  │ 名称·状态 | 方式 | 关键标识 | 登录页 | 更新时间 │ │
  │ │ 微信 / 钉钉   │  │                                编辑 / 更多     │ │
  │ │ 企业微信     │  │ 空时：暂无配置，立即新增                      │ │
  │ │ OAuth2       │  └───────────────────────────────────────────────┘ │
  │ └───────────────┘                                                    │
  └───────────────────────────────────────────────────────────────────────┘
  ```

  信息密度为 6–7 列、一行 1–2 常用动作；方式与状态以平台徽标表达，不用单侧彩条。`EqualHeightColumns` + 固定方式侧栏 + `a-table`/平台表格能力 + `CloudEmpty` + `JlDrawerShell` + Ant 表单/按钮/开关为优先组件，实施时核对真实契约。状态、登录页展示与钉钉同步用列表/表单局部开关并反馈；凭据集合才打开抽屉，编辑方式不漂移。主操作在列表标题右侧，编辑/停用在行内；不新增第二层应用导航、KPI、banner、登录页预览、卡片墙、装饰图。

## Risks / open questions
- 现有 `system/Apply` 配置的是“应用/单点登录”业务，已有第三方应用 OAuth2 与用户绑定入口；新页第一阶段是单独的“本平台登录身份源配置”交互，不复用其 API 或数据，也不改旧配置。第二阶段先比对重叠能力，再决定底层复用、迁移或并存，避免先造第二套冲突的持久化模型。
- 当前只有前端页面契约；真实接口、密钥存储与回显策略、凭据校验、回调地址、组织同步周期、权限以及登录审计的数据模型将在第二/三阶段单独设计并确认，不在第一阶段假定后端已支持。
- 新增菜单模板若在生产环境被同步，会让未接接口页面可访问；第一阶段仅本地/评审环境同步、展示与验收，不纳入生产发布。开发完成时检查菜单同步的实际路径、权限及部署风险并向用户明确报告。
- 浏览器安全策略拒绝打开用户提供的本地 `file://` 页面；其结构依据 HTML 源码核对，第一阶段验收以本平台页面实际渲染为准。

## Out of scope
- 第一阶段不改任何后端、真实登录页、现有 `system/Apply` 单点登录、共享组件实现或其他前端模块。
- 不做真实配置持久化、认证跳转、回调处理、组织同步任务、上传接口、登录日志/审计接口或上线发布。

## 补充计划（待用户确认）：菜单归位与 SaaS 授权数据

目标：第一阶段的配置页仍只作本地/评审预览，但其菜单应位于“开放与集成”；运营端 auth 菜单模板应包含对应定义，并让 SaaS 运营端能够在目标运行时的服务授权树中选到该菜单。保留“单点登录”、已有菜单 ID/路由与其他授权。

影响范围：`runtime-ui/modules/authentication-manager-ui/baseMenu.json`、`ui/modules/authentication-manager-ui/baseMenu.json`，以及必要的 `ui/modules/saas-manager-ui` 区域菜单同步入口/验证；不改页面布局、配置接口或认证后端模型。运行时 auth 菜单的持久化由现有区域菜单同步接口承担，不通过修改静态 JSON 假装已经写入数据库。

实施步骤与验证：
1. 将运行端第三方登录节点从“系统管理”移到“开放与集成”的 `children`，保留 `system/ThirdPartyLogin` code 与现有 URL（页面仍由 `views/system/ThirdPartyLogin/index.vue` 动态发现），不移动或复制页面。验证其父级、ID 唯一、原单点登录仍在原处及路由映射。
2. 在运营端 auth 菜单模板同一父级补齐同 code、ID、URL、名称的运行时菜单定义；核对 `owner`/`runtime`、初始化菜单与无运营端页面时的路由可见性，确保该节点可供 SaaS 默认菜单同步且不会生成可点却无页面的运营端路由。必要时使用现有运行时菜单候选机制，而不是复制一份运营端页面。验证两端 JSON 与候选集合一致。
3. SaaS 授权页的菜单树来自运行时 auth 服务的持久菜单，不直接读前端 JSON。核验现有 `saveDefaultRuntimeMenus` 对该节点及父级的选择、owner 分组和父子关系；若已有区域缺少可触发的同步入口，补可见且需确认的“同步默认菜单”操作，明确其 cloud/app 全量替换风险。仅由授权管理员在本地/评审目标区域执行同步，不自动写入生产运行时；同步后查 `menus/_tree`，再在服务授权编辑器中确认新节点可选，不自动授予任何服务。
4. 为双端菜单父级/ID/路由及 SaaS 同步候选写聚焦回归；运行相应 JSON、前端构建/测试，核对菜单同步不会删除既有“单点登录”或改变其他授权。更新原模块 README 的菜单说明与验证/未验证风险。

风险和待确认：运营端模板的 `owner: iot` 节点目前被 `baseMenu.ts` 标为 `runtime: false`，SaaS 默认同步只收 `runtime` 节点；简单复制现有第三方节点并不能出现在授权树。现有同步 API 会按 owner 全量替换区域菜单，不能未经核对对生产区域执行。此前“仅本地/评审环境同步”的范围继续有效；本增补不代表第三方登录配置接口或真实登录已可用。

实施结果：运行端菜单已归入“开放与集成”，运营端 auth 菜单增加 `owner: cloud`、`runtimeOnly: true` 候选；运营端默认初始化及微应用 JSON 动态加载均过滤该标记，SaaS 同步时去标记。SaaS 授权抽屉提供带全量替换风险提示的确认入口；同步仅提交 runtime 节点，跨 owner 子菜单引用已有 IoT 父菜单，父级不存在则拒绝同步。双端菜单、运营端过滤与 SaaS 纯选择逻辑共 9 项测试通过，三个前端模块构建通过；未向任何真实区域发起同步，目标区域的持久菜单与授权编辑器仍须由管理员在本地/评审环境验收。
